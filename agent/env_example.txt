# Pydantic AI Agent Environment Configuration

# OpenAI API Key (required for the agent to function)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Customize the agent URL (defaults to http://localhost:8000)
PYDANTIC_AI_AGENT_URL=http://localhost:8000

# Optional: LangSmith API Key for tracing (if using LangSmith)
LANGSMITH_API_KEY=your_langsmith_api_key_here 