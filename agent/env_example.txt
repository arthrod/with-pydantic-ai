# AI Research Agent Environment Configuration
# Copy this file to .env and fill in your API keys

# Required: OpenAI API Key for the language model
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Tavily API Key for enhanced web search capabilities  
# Get your key from: https://tavily.com (currently using DuckDuckGo as fallback)
TAVILY_API_KEY=your_tavily_api_key_here

# Optional: Set to 1 to enable debug logging
PYDANTIC_AI_DEBUG=0

# Optional: Custom model configuration (default: gpt-4o)
# OPENAI_MODEL=gpt-4o-mini

# Optional: Custom port for the agent server (default: 8000)
# AGENT_PORT=8000 